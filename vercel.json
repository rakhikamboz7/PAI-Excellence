{
  "version": 2,
  "buildCommand": "pnpm install && pnpm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
